services
  - type: web
  name: titanic-predictor-model
  env: python
  buildCommand: pip install -r requirements.txt
  startCommand: python -m uvicorn main:app --host 0.0.0.0 --port $PORT
  rootDir: day99
  plan: free
